# 🍕 PizzaShop — Доставка еды

Современное мобильное приложение для заказа пиццы, разработанное на **SwiftUI** с использованием **Firebase** в качестве серверной части. Проект включает в себя полноценный клиентский опыт и панель управления для администратора.

## 🌟 Основные возможности

### Клиентская часть
* **Авторизация и профиль:** Регистрация и вход через Firebase Auth.
* **Меню:** Динамический список товаров с фильтрацией по категориям.
* **Корзина:** Удобное управление выбранными позициями и расчет стоимости.
* **Оформление заказа:** Интеграция с базой данных для сохранения истории заказов.

### Панель Администратора
* **Управление статусами:** Возможность менять статус заказа (Новый -> Готовится -> Доставлен).
* **Контроль товаров:** Добавление новых позиций в меню и редактирование текущих в реальном времени.
* **История всех заказов:** Просмотр активности всех пользователей.

## 🛠 Технологический стек

* **UI Framework:** SwiftUI (Declarative UI)
* **Архитектура:** MVVM (Model-View-ViewModel)
* **Backend:** * **Firebase Authentication** (управление пользователями).
    * **Cloud Firestore** (хранение меню и заказов в NoSQL формате).
    * **Firebase Storage** (хранение изображений блюд).
* **Async/Await:** Современная работа с сетевыми запросами.

## 🏗 Логическая структура проекта

Проект разделен на четкие слои для упрощения поддержки:
* `Models`: Описание структур данных (User, Product, Order).
* `ViewModels`: Бизнес-логика, связь View с Firebase-сервисами.
* `Views`: Компоненты интерфейса, разделенные на `Client` и `Admin` подпапки.
* `Services`: Выделенные классы для работы с базой данных (`DatabaseService`, `AuthService`).

---
*Проект выполнен в учебных целях/в рамках тестового задания.*

